<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Athlete</title>
    <!--Import the stylesheets from the styling folder-->
    <link rel="stylesheet" href="/assets/css/style.css">
    <style>
        /* Sets the background color for the page */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 0;
        }

        /* Sets styling for the title */
        h1 {
            text-align: center;
            margin-top: 20px;
            color: #333;
        }

        /* Styling for the form */
        form {
            width: 50%;
            margin: 20px auto;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 10px;
            background-color: #F9F9F9;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        /* Sets styling for the titles of each input field */
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #555;
        }

        /*Sets styling for the rest of the form */
        input, select, button, textarea {
            width: 100%;
            padding: 8px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        /* Sets styling for the submit button */
        button {
            background-color: #4CAF50;
            color: white;
            font-size: 16px;
            cursor: pointer;
            border: none;
            transition: background-color 0.3s ease;
        }

        /* Allows for changing of the color of the submit button on hovering over it */
        button:hover {
            background-color: #45A049;
        }

        /* puts margin after the end of the radio group */
        .radio-group {
            margin-bottom: 15px;
        }

        /* Puts margin next to the radio group selection so the buttons are not directly next to them */
        .radio-group label {
            margin-right: 15px;
        }

        /*CSS for the return to home */
        .btn-back {
            display: block;
            text-align: center;
            margin: 20px auto;
            color: #007bff;
            text-decoration: none;
            font-weight: bold;
        }

        /*Hover CSS for the return button */
        .btn-back:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <!-- Only the user logged in with admin access can view this page-->
    <!-- security placeholder -->
    <h1>Add Athlete</h1>
    <!-- Form for adding a user -->
    <form action="/addAthlete" method="POST">
        <h3>Athlete Information</h3>
        <!-- First name text input field -->
        <div>
            <label for="athfirstname">First Name</label>
            <input type="text" id="athfirstname" name="athfirstname" placeholder="Enter athlete's first name" required>
        </div>

        <!-- Last name text input field -->
        <div>
            <label for="athlastname">Last Name</label>
            <input type="text" id="athlastname" name="athlastname" placeholder="Enter athlete's last name" required>
        </div>

        <label for="schoolid">Select School:</label>
        <select name="schoolid" id="schoolid" required>
            <% schools.forEach(school => { %>
                <option value="<%= school.schoolid %>"><%= school.schooldescription %></option>
            <% }); %>
        </select>

        <label for="employeeid">Select Employee:</label>
        <select name="employeeid" id="employeeid" required>
            <% employees.forEach(employee => { %>
                <option value="<%= employee.employeeid %>"><%= employee.empfirstname %> <%= employee.emplastname %></option>
            <% }); %>
        </select>
        
        <!-- Email input field, must be email -->
        <div>
            <label for="email">Email</label>
            <input type="email" id="email" name="email" placeholder="Enter the athlete's email">
        </div>

        <!-- Phone number input field, only allows numbers from 0-9 and it must have ten digits-->
        <div>
            <label for="phonenumber">Phone Number</label>
            <input type="tel" id="phonenumber" name="phonenumber" placeholder="Enter phone number" pattern="[0-9]{10}" title="Enter a valid 10-digit phone number" required>
        </div>

        <h3>Basketball Stats</h3>
        <div>
            <label for="blocks">Blocks</label>
            <input type="number" id="blocks" name="blocks" placeholder="Enter the number of blocks">
        </div>
        
        <div>
            <label for="steals">Steals</label>
            <input type="number" id="steals" name="steals" placeholder="Enter the number of steals">
        </div>
        
        <div>
            <label for="turnovers">Turnovers</label>
            <input type="number" id="turnovers" name="turnovers" placeholder="Enter the number of turnovers">
        </div>
        
        <div>
            <label for="twopointers">2-pointers</label>
            <input type="number" id="twopointers" name="twopointers" placeholder="Enter the number of 2-pointers">
        </div>
        
        <div>
            <label for="threepointers">3-pointers</label>
            <input type="number" id="threepointers" name="threepointers" placeholder="Enter the number of 3-pointers">
        </div>
        <h3>Football Stats</h3>
        <div>
            <label for="tackles">Tackles</label>
            <input type="number" id="tackles" name="tackles" placeholder="Enter the number of tackles">
        </div>
        
        <div>
            <label for="catches">Catches</label>
            <input type="number" id="catches" name="catches" placeholder="Enter the number of catches">
        </div>
        
        <div>
            <label for="sacks">Sacks</label>
            <input type="number" id="sacks" name="sacks" placeholder="Enter the number of sacks">
        </div>
        
        <div>
            <label for="interceptions">Interceptions</label>
            <input type="number" id="interceptions" name="interceptions" placeholder="Enter the number of interceptions">
        </div>
        
        <div>
            <label for="touchdowns">Touchdowns</label>
            <input type="number" id="touchdowns" name="touchdowns" placeholder="Enter the number of touchdowns">
        </div>
        

        <!-- Submit Button -->
        <button type="submit" class="btn btn-primary btn-block">Add Athlete</button>
    </form>

    <!-- Returns to the maintenance page for volunteers -->
    <a href="/showAthlete" class="btn-back">Back to Athlete</a>
    <!-- Denies the login if the user is not logged in, or if they do not have admin access -->
    <!-- else statement security placeholder %> -->
</body>
</html>
